# =============================================================================
# 云端 Actions 依赖（R14）
# =============================================================================
# 只列云端 *必需* 的包，保持干净。
#
# 【为什么锁定精确版本】
#   未锁版本时，上游一次破坏性更新就可能让 Actions 静默跑挂——而 Actions
#   是定时任务，挂了不会有人立刻发现，可能几天后才从看板数据缺失察觉。
#   锁定版本换取可复现性，是这类无人值守任务的基本原则。
#
#   升级方式：改这里的版本号 → 手动 workflow_dispatch 跑一次验证 → 提交。

aiohttp==3.13.3

# -----------------------------------------------------------------------------
# 以下**不**列入云端依赖，说明原因：
#
# requests
#   probe_v4.py 里 requests 只是可选的降级后端（aiohttp 不可用时才用），
#   云端有 aiohttp 就够。**手机侧（Termux）的 probe_local.py 需要它**，
#   见 tools/requirements-phone.txt。
#
# 其余（json / re / subprocess / datetime 等）
#   全部是 Python 3.11 标准库，无需声明。
# -----------------------------------------------------------------------------
